# T014 Live Handoff — 2026-03-21

## Scope and topology

Keep the current live topology exactly as-is:
- router on `.100` via Docker Compose only
- workers on `.111` via systemd only
- no dual supervisor on `.100`

This handoff is for the `gobabygo` team. It documents the current live findings after triage, not a platform redesign.

## Repo/runtime state

Source-of-truth repo remains `origin/master`.
During triage, the live codex session-worker runtime under `/opt/mesh-worker` was temporarily patched for reproduction. Before writing this handoff, the live runtime was restored to the current repo `HEAD` and the codex session worker was restarted, so runtime and repo are aligned again.

## What is already materially improved

The following earlier fixes are real and useful, even though T014 is still open:
- `d9eddaa` `fix(mesh): increase session worker control-plane budget`
- `e9c961f` `fix(mesh): honor control-plane timeout in cli`
- `9462a71` `fix(mesh): wait longer for ui group closure`
- `1002b2f` `fix(mesh): observe ui close without local cache`
- `7407278` `fix(mesh): force close terminal ui sessions`
- `e284070` `fix(mesh): route ui codex tasks to work-codex`
- `a01205e` `fix(mesh): bootstrap codex ui sessions at launch`

Operationally, this means:
- stale `snake-game` UI groups can be closed deterministically with `mesh ui close`
- `mesh sessions snake-game` no longer times out at the router boundary
- `worker-codex` tasks now route to `target_account=work-codex` instead of the wrong account

## Current verdict

`T014` is still `NO-GO`.

Reason:
- backend/runtime behavior is still not reliable enough for acceptance
- current cockpit behavior is not the expected full role-session outcome
- in the latest tests, only worker lanes open reliably
- `boss` and `president` are not considered correctly spawned/verified from an operator point of view

## Live findings from this session

### 1. `worker-codex` is still not stable under the real session-worker service path

Observed live behavior:
- fresh `mesh ui /media/sam/1TB/snake-game` runs create `worker-codex` tasks
- routing is correct: `assigned_worker=ws-codex-session-01`, account profile `work-codex`
- but the session-worker service still fails the codex task shortly after start

Representative task ids seen during this triage:
- `687a5207-738b-41aa-91ab-ee92531e7208`
- `1aeac25c-ad3e-407f-924e-f1815b8216b8`
- `bb08365d-c3ec-4327-8314-62abbcc29ee0`
- `bc32e12e-055d-46c2-b2bb-38127e3854c6`

Representative journal failure:
- unit: `mesh-session-worker@mesh-session-codex-work`
- error: `CalledProcessError` from `tmux send-keys ... returned non-zero exit status 1`

Interpretation:
- the `codex` pane is disappearing before the service can reliably inject the bootstrap prompt
- this is still a service/runtime-context issue, not a router-topology issue

### 2. Manual reproduction as `mesh-worker` succeeds

A direct manual reproduction as `mesh-worker` on `.111` succeeds:
- `tmux new-session ... "ccs codex"`
- after a short delay, `tmux send-keys` with the full bootstrap prompt keeps the session alive

This was also true for:
- `ccs codex`
- `ccs codex --effort xhigh`
- short and long prompts
- delays `0`, `0.1`, `0.35`, and `0.5`

Interpretation:
- Codex itself is not disproven as a session CLI
- the remaining bug is the exact service execution context/path, not the basic tmux+codex combination

### 3. Current operator-visible result is still not acceptable

Latest reruns showed that the system can still materialize some roles, but not the full cockpit correctly.

Observed/confirmed during this session:
- worker lanes are the only roles that open reliably in tests
- `boss` is not showing up as a router-backed live session in the current checks
- `president` is not considered reliably/acceptably spawned from the operator perspective
- therefore the cockpit-level acceptance remains open even when some backend sessions exist

### 4. Router-side health is not the main blocker anymore

The earlier hard router timeout on:
- `./scripts/mesh sessions snake-game`

was removed during triage.
The remaining blocker is narrower:
- role-session spawn/lifecycle correctness under the real worker service path

## Important scope clarification

There is an open product/acceptance question from the operator side:
- if `T014` is supposed to be accepted with `ccs gemini` as the canonical worker path, then current `codex`-specific failures should be tracked separately from the acceptance gate
- if `T014` still requires a real `worker-codex` live role session, then the current state remains a hard blocker

This needs to be clarified explicitly by the `gobabygo` team. Do not let this remain implicit.

## Recommended next steps for the gobabygo team

1. Re-check the canonical acceptance contract for `T014`.
   - Decide explicitly whether `worker-codex` is part of the acceptance bar, or whether `ccs gemini` is the intended canonical worker for this flow.

2. If `worker-codex` is still in scope, debug the service-context delta only.
   - Compare the working manual `mesh-worker` tmux launch against the failing systemd session-worker path.
   - Focus on environment/runtime differences, not topology changes.
   - Good first suspects: exact environment, startup sequencing, and any difference between manual tmux launch and the service-managed path.

3. Keep topology unchanged.
   - Do not move router supervision.
   - Do not introduce dual supervisors.
   - Do not redesign around this failure before the service-context mismatch is understood.

4. Treat operator-visible spawn as the real acceptance bar.
   - Backend task/session existence alone is not enough.
   - The current operator report is that only workers open reliably; `boss` and `president` are still not acceptable.

## Minimal evidence captured in this session

Healthy/manual evidence:
- manual `mesh-worker` tmux launch with `ccs codex` stayed alive after prompt injection
- `mesh ui close` successfully cleared stale `snake-game` groups
- `mesh sessions snake-game` returned promptly instead of timing out

Failure evidence:
- repeated `worker-codex` tasks failed under `mesh-session-worker@mesh-session-codex-work`
- journal showed `tmux send-keys` hitting a dead pane/session during bootstrap
- latest tests were still not acceptable at cockpit level because only worker lanes were reliably opening

## Handoff summary

Do not treat T014 as complete.
The remaining work should happen in the `gobabygo` repo and runtime ownership, with a narrow focus on:
- service-context mismatch for codex role sessions
- explicit acceptance scope (`ccs gemini` vs `ccs codex`)
- operator-visible spawn correctness for `boss` and `president`
