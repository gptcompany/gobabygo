# Mesh UI Role Sessions T014 Handoff — 2026-03-22

## Executive Summary

`T014` is still open.

The current repo and live system do **not** support the claim that
`specs/mesh-ui-role-sessions/` is at a completed `v1.4` milestone. In this repo,
the mesh UI role sessions work is tracked as `T001` through `T014`, with `T014`
remaining the final acceptance gate.

The current canonical live router is **not** `.100`. It is:

- router: `10.0.0.4:8780` on `vpsazure`, reachable over VPN
- worker host: `.111` / `192.168.1.111`

Older references to router `.100` in this repo are now historical and should not
be treated as the source of truth for current T014 debugging.

## What Is Correct vs Incorrect

### Incorrect / unsupported

These statements are **not** grounded in the current mesh UI role sessions spec:

- `v1.0–v1.4 completed (21 phases, 548+ tests, 6742+ LOC)`
- `post-v1.4 not planned`

There is no version ladder like `v1.0 ... v1.4` in:

- `specs/mesh-ui-role-sessions/spec.md`
- `specs/mesh-ui-role-sessions/plan.md`
- `specs/mesh-ui-role-sessions/tasks.md`

The main work breakdown in the current spec is:

- `T001` ... `T014`

The `v1.4` references present elsewhere in the repo are about deployment/topology
files such as `deploy/topology.v1.4.example.yml`, not the mesh UI role sessions
feature lifecycle.

### Partially outdated

This older blocker set is now stale if read as the **current** T014 blocker list:

1. `Router instability` on `.100`
2. `Bootstrap race`
3. `iTerm2 verification`

Why it is stale:

- `.100` is no longer the canonical live router for the current mesh UI work.
- the bootstrap-race hypothesis was already documented as resolved in the
  `2026-03-20` findings, and it is no longer the primary blocker in current runs.
- iTerm2 verification is still noisy, but pane metadata tagging is now improved;
  the current unresolved problem is more specific than generic iTerm2 extraction.

## Current T014 State

### Authoritative status

- `T014` remains open.
- Local Mac regression/unit coverage for the launcher currently passes:
  - `pytest -q tests/test_mesh_ui_script.py` -> `57 passed`
- This is **not** sufficient for acceptance.
- Final acceptance still depends on a live operator-visible pane proof.

### Current live topology

- canonical router: `http://10.0.0.4:8780`
- worker host: `.111`
- `.100` should be treated as historical context for older findings only

### Current local branch state

As of `2026-03-22`, the local Mac branch contains these unpushed commits on top of
`origin/master`:

- `b7cb45a` `fix(ui): robust role task spawning and repo-aware iTerm2 cleanup`
- `48f9fed` `test(mesh-ui): align repo-aware iTerm session coverage`
- `175516d` `fix(ui): robust retry on task spawn 409 collision`
- `91af47f` `fix(ui): resolve layout initialization timeouts via router poll optimizations`

### Current live symptom progression

Observed progression across recent local runs:

1. initial failure mode:
   - `duplicate ui role task for <role> but existing task was not found`
2. later failure mode:
   - `session spawn timeout after 60s`
3. current user-visible state:
   - operator still reports seeing shells instead of visible live CLI prompts in
     the role panes

This means recent fixes have changed the failure mode, but T014 is still not
proven accepted.

## Verified Live Evidence

For a recent live UI group:

- `ui_group_id = progressive-deploy-ui-20260321T111430Z`

Router state on `.111` showed:

- `president` task in `running` with a non-null `session_id`
- `lead` task in `running` with a non-null `session_id`
- `worker-gemini` task in `running` with a non-null `session_id`
- `verifier` only in `canceled` state with `session_id = null`

Interpretation:

- the control plane is capable of creating at least some live UI role tasks and
  assigning them a session
- the unresolved gap is likely in the final attach/render/verification path on the
  Mac launcher side, not only in raw task creation

## Most Useful Resume Order For An External Agent

An external agent such as Claude Code should read these files in this order:

1. `specs/mesh-ui-role-sessions/spec.md`
2. `specs/mesh-ui-role-sessions/plan.md`
3. `specs/mesh-ui-role-sessions/tasks.md`
4. `specs/mesh-ui-role-sessions/t014-findings-20260320.md`
5. `specs/mesh-ui-role-sessions/t014-handoff-20260322.md`
6. `scripts/mesh_iterm_ui.py`
7. `tests/test_mesh_ui_script.py`
8. `scripts/mesh_ui_role_shell.sh`

## Correct Reproduction Command

Use the explicit roles flag. Do not pass the roles as the positional `repo`
argument.

For `progressive-deploy`:

```bash
zsh -lc 'cd /Users/sam/gobabygo && source ~/.mesh/router.env && export UV_CACHE_DIR=/tmp/uv-cache && uv run --with iterm2 scripts/mesh_iterm_ui.py progressive-deploy --roles boss,president,lead,worker-gemini,verifier'
```

## Acceptance Criteria That Must Be Used

Do **not** accept T014 based on:

- green unit tests alone
- successful tab creation alone
- successful pane metadata tagging alone
- router task creation alone

Accept T014 only if a live run proves:

- `boss` is an operator shell
- `president` shows a real live CLI prompt
- `lead` shows a real live CLI prompt
- `worker-gemini` shows a real live CLI prompt
- `verifier` is either a real live CLI prompt or is explicitly blocked by direct
  evidence
- pane content proof is based on actual pane contents, not just process exit codes

## What An External Agent Should Not Assume

Do not assume:

- that `.100` is still the active router for T014
- that the `v1.4` topology files define mesh UI feature maturity
- that `T014` was closed by any of the recent local UI commits
- that a changed error message means successful live CLI attach

