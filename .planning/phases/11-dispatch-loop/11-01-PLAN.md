---
phase: 11-dispatch-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/router/server.py
  - src/router/metrics.py
  - tests/router/test_server.py
  - tests/smoke/test_e2e_live.py
autonomous: true
requirements: [OPRDY-01, OPRDY-02]

must_haves:
  truths:
    - Dispatch loop thread runs in run_server() after scheduler init
    - All dispatchable tasks are drained per cycle (inner while True)
    - Dispatch interval configurable via MESH_DISPATCH_INTERVAL_S env var
    - Exception in dispatch does not crash the thread
  artifacts:
    - 2 new Prometheus counters (dispatch_cycles_total, tasks_dispatched_total)
    - 2 new tests in test_server.py
    - Updated E2E test without manual scheduler.dispatch()
  key_links:
    - server.py dispatch_loop -> scheduler.dispatch()
    - server.py dispatch_loop -> metrics.dispatch_cycles_total
    - server.py dispatch_loop -> metrics.tasks_dispatched
---

<objective>Add periodic dispatch loop daemon thread to run_server() that automatically assigns queued tasks to idle workers</objective>

<execution_context>
GSD execute-plan workflow. Summary template: 11-01-SUMMARY.md
</execution_context>

<context>
- .planning/PROJECT.md -- Project overview and decisions
- .planning/ROADMAP.md -- Phase progress tracking
- src/router/server.py -- run_server() with existing daemon threads
- src/router/scheduler.py -- dispatch() method
- src/router/metrics.py -- Prometheus metrics
- tests/router/test_server.py -- Existing server tests
- tests/smoke/test_e2e_live.py -- E2E smoke test
</context>

<tasks>
  <task type="auto">
    <name>Add dispatch metrics to MeshMetrics</name>
    <files>src/router/metrics.py</files>
    <action>
    Add two new Counter metrics to MeshMetrics.__init__():
    1. `dispatch_cycles_total` - Counter with label `result` (dispatched/empty/error)
    2. `tasks_dispatched` - Counter (no labels)
    </action>
    <verify>python -c "from src.router.metrics import MeshMetrics; m = MeshMetrics(); assert 'dispatch_cycles' in m.generate().decode()"</verify>
    <done>Two dispatch counters registered in MeshMetrics</done>
  </task>

  <task type="auto">
    <name>Add dispatch_loop daemon thread in run_server()</name>
    <files>src/router/server.py</files>
    <action>
    After scheduler init and before review_check_loop, add:
    1. Read MESH_DISPATCH_INTERVAL_S from env (default 5)
    2. Define dispatch_loop() function:
       - sleep(dispatch_interval)
       - Inner while True: call scheduler.dispatch() until None
       - Count dispatched tasks, log each dispatch
       - Inc metrics.tasks_dispatched per task, metrics.dispatch_cycles_total per cycle
       - Exception-safe: log error, inc error counter, continue
    3. Start as daemon thread named "dispatch"
    </action>
    <verify>grep -n "dispatch_loop" src/router/server.py</verify>
    <done>Dispatch loop thread starts in run_server()</done>
  </task>

  <task type="auto">
    <name>Add dispatch loop tests and update E2E</name>
    <files>tests/router/test_server.py, tests/smoke/test_e2e_live.py</files>
    <action>
    In test_server.py:
    1. TestDispatchLoop.test_dispatch_loop_dispatches_queued_task -- single task+worker, verify assigned after 0.5s
    2. TestDispatchLoop.test_dispatch_loop_multiple_tasks -- 2 tasks+2 workers, all assigned

    In test_e2e_live.py:
    1. Replace manual scheduler.dispatch() with dispatch_loop thread (interval 0.1s)
    2. Remove dispatch_thread.join()
    3. Verify task still arrives via long-poll within 2s
    </action>
    <verify>python -m pytest tests/router/test_server.py::TestDispatchLoop tests/smoke/test_e2e_live.py -v</verify>
    <done>4 tests pass (2 new dispatch loop + 2 updated E2E)</done>
  </task>
</tasks>

<verification>
1. `python -m pytest tests/router/test_server.py::TestDispatchLoop -v` -- 2 tests pass
2. `python -m pytest tests/smoke/test_e2e_live.py -v` -- E2E passes without manual dispatch
3. `grep "dispatch_loop" src/router/server.py` -- function exists
4. `grep "MESH_DISPATCH_INTERVAL_S" src/router/server.py` -- env var read
5. `grep "dispatch_cycles_total" src/router/metrics.py` -- metric registered
</verification>

<success_criteria>
1. Dispatch loop thread runs as daemon in run_server()
2. All queued tasks dispatched to idle workers within one cycle
3. Interval configurable via MESH_DISPATCH_INTERVAL_S
4. E2E test passes with dispatch loop (no manual dispatch)
5. All 435 tests pass
</success_criteria>

<output>Create 11-01-SUMMARY.md with execution results</output>
