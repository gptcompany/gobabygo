---
phase: 12-post-tasks
plan: 01
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src/router/models.py
  - src/router/server.py
  - src/router/metrics.py
  - src/meshctl.py
  - tests/router/test_server.py
  - tests/test_meshctl.py
autonomous: true
requirements: [OPRDY-03, OPRDY-04, OPRDY-05]

must_haves:
  truths:
    - POST /tasks accepts TaskCreateRequest and returns 201
    - Client cannot set internal fields (status, assigned_worker)
    - Duplicate idempotency_key returns 409
    - Eager dispatch called after insert
    - meshctl submit creates tasks via HTTP
  artifacts:
    - TaskCreateRequest model in models.py
    - _handle_create_task handler in server.py
    - cmd_submit function in meshctl.py
    - 2 new Prometheus counters (tasks_created, tasks_create_errors)
    - 9 new server tests, 7 new meshctl tests
  key_links:
    - server._handle_create_task -> TaskCreateRequest -> Task -> db.insert_task
    - server._handle_create_task -> scheduler.dispatch (eager)
    - meshctl.cmd_submit -> POST /tasks
---

<objective>Add POST /tasks HTTP endpoint with TaskCreateRequest DTO, duplicate detection, eager dispatch, and meshctl submit CLI command</objective>

<execution_context>
GSD execute-plan workflow. Summary template: 12-01-SUMMARY.md
</execution_context>

<context>
- .planning/PROJECT.md -- Project overview
- src/router/models.py -- Task model
- src/router/server.py -- HTTP handlers
- src/router/db.py -- insert_task with UNIQUE constraint on idempotency_key
- src/meshctl.py -- CLI tool
</context>

<tasks>
  <task type="auto">
    <name>Add TaskCreateRequest model and creation metrics</name>
    <files>src/router/models.py, src/router/metrics.py</files>
    <action>
    In models.py: Add TaskCreateRequest(BaseModel) with public fields only:
    - title (str, required), phase, payload, target_cli, target_account, priority,
      depends_on, deadline_ts, not_before, critical, idempotency_key
    - Default target_account="work" (not "default")

    In metrics.py: Add tasks_created (Counter) and tasks_create_errors (Counter, labels: reason)
    </action>
    <verify>python -c "from src.router.models import TaskCreateRequest; r = TaskCreateRequest(title='test'); print(r.model_dump())"</verify>
    <done>TaskCreateRequest DTO and creation metrics registered</done>
  </task>

  <task type="auto">
    <name>Add POST /tasks handler with duplicate detection and eager dispatch</name>
    <files>src/router/server.py</files>
    <action>
    1. Add route: POST /tasks -> _handle_create_task()
    2. Import TaskCreateRequest, Task, sqlite3
    3. Handler: auth check -> read body -> parse JSON -> validate TaskCreateRequest ->
       convert to Task -> insert_task -> catch IntegrityError (409) ->
       eager dispatch (best-effort) -> 201 with task_id
    4. Log task_created with id and title
    </action>
    <verify>grep "_handle_create_task" src/router/server.py</verify>
    <done>POST /tasks handler with full error handling</done>
  </task>

  <task type="auto">
    <name>Add meshctl submit subcommand</name>
    <files>src/meshctl.py</files>
    <action>
    1. Add cmd_submit(args) function:
       - Build JSON body from args (title, cli, account, phase, priority, payload)
       - POST /tasks with auth headers
       - Handle 201 (print task_id), 401, 409, other errors
    2. Add submit_parser with --title (required), --cli, --account, --phase, --priority, --payload
    3. Add elif in __main__ block
    </action>
    <verify>python -m src.meshctl submit --help</verify>
    <done>meshctl submit creates tasks via HTTP</done>
  </task>

  <task type="auto">
    <name>Add tests for POST /tasks and meshctl submit</name>
    <files>tests/router/test_server.py, tests/test_meshctl.py</files>
    <action>
    Server tests (9):
    - test_post_tasks_creates_task (201 + DB)
    - test_post_tasks_auth_required (401)
    - test_post_tasks_invalid_json (400)
    - test_post_tasks_invalid_fields (400)
    - test_post_tasks_missing_title (400)
    - test_post_tasks_duplicate_idempotency_key (409)
    - test_post_tasks_ignores_internal_fields
    - test_post_tasks_triggers_dispatch
    - test_post_tasks_with_payload

    meshctl tests (7):
    - test_submit_success
    - test_submit_with_payload
    - test_submit_invalid_payload_json
    - test_submit_auth_failure
    - test_submit_duplicate_409
    - test_submit_connection_error
    - test_submit_with_all_options
    </action>
    <verify>python -m pytest tests/router/test_server.py::TestPostTasksEndpoint tests/test_meshctl.py::TestSubmitCommand -v</verify>
    <done>16 new tests pass</done>
  </task>
</tasks>

<verification>
1. `python -m pytest tests/router/test_server.py::TestPostTasksEndpoint -v` -- 9 tests pass
2. `python -m pytest tests/test_meshctl.py::TestSubmitCommand -v` -- 7 tests pass
3. `grep "class TaskCreateRequest" src/router/models.py` -- DTO exists
4. `grep "POST /tasks" src/router/server.py` or `grep "_handle_create_task" src/router/server.py`
5. `grep "cmd_submit" src/meshctl.py` -- submit command exists
</verification>

<success_criteria>
1. POST /tasks creates task with 201 response
2. Invalid input returns 400, duplicate returns 409
3. Internal fields (status, assigned_worker) cannot be set by client
4. Eager dispatch assigns task to idle worker immediately
5. meshctl submit works with all options
6. All 435 tests pass
</success_criteria>

<output>Create 12-01-SUMMARY.md with execution results</output>
