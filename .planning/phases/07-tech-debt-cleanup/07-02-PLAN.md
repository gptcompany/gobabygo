# Plan 07-02: YAML Mapping + mypy Fix

**Phase:** 07-tech-debt-cleanup
**Requirements:** DEBT-02, DEBT-03
**Goal:** Complete YAML mapping for gsd:implement-* commands; fix mypy annotation in heartbeat.py

## DEBT-02: YAML Semantic Mapping

### Current State

`mapping/command_rules.yaml` implement rule:
```yaml
- match: "^gsd:execute-.*|^pipeline:gsd$|^speckit\\.implement.*"
  step: implement
```

Missing: `gsd:implement-*` pattern (e.g., `gsd:implement-plan`, `gsd:implement-fix`, `gsd:implement-phase-sync`)

### Changes

**File:** `mapping/command_rules.yaml`

Update implement rule regex to include `gsd:implement-*`:
```yaml
- match: "^gsd:execute-.*|^gsd:implement-.*|^pipeline:gsd$|^speckit\\.implement.*"
  step: implement
```

**File:** `tests/router/test_mapping.py`

Add test cases:
- `gsd:implement-plan` → step: implement
- `gsd:implement-fix` → step: implement
- `gsd:implement-phase-sync` → step: implement

## DEBT-03: mypy Fix

### Current State

`src/router/heartbeat.py:187` uses `Worker` type annotation but doesn't import it.
Import line 15: `from src.router.models import TaskEvent, TaskStatus`
Missing: `Worker` in imports.

`from __future__ import annotations` (line 6) prevents runtime error, but mypy reports:
```
src/router/heartbeat.py:187: error: Name "Worker" is not defined  [name-defined]
```

### Changes

**File:** `src/router/heartbeat.py` line 15

Change:
```python
from src.router.models import TaskEvent, TaskStatus
```
To:
```python
from src.router.models import TaskEvent, TaskStatus, Worker
```

## Verification

- [ ] `mypy src/` passes with zero errors
- [ ] `pytest tests/router/test_mapping.py -v` passes with new test cases
- [ ] `pytest tests/router/test_heartbeat.py -v` passes (no regression)

## Risk

- **Minimal**: Both changes are additive (new regex pattern, new import)
