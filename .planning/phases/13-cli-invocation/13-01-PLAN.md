---
phase: 13-cli-invocation
plan: 01
type: execute
wave: 1
depends_on: [12-01]
files_modified:
  - src/router/worker_client.py
  - src/router/metrics.py
  - tests/router/test_worker_client.py
autonomous: true
requirements: [OPRDY-06, OPRDY-07, OPRDY-08]

must_haves:
  truths:
    - _execute_task validates payload.prompt and fails if missing
    - dry_run mode reports complete without subprocess
    - Every error path calls _report_failure (no stuck tasks)
    - subprocess.run called with capture_output=True, no shell=True
    - Output truncated (4KB stdout, 2KB stderr)
    - Config fields read from env vars
  artifacts:
    - Rewritten _execute_task with subprocess invocation
    - Extracted _ack_task, _report_complete, _report_failure helpers
    - 4 new WorkerConfig fields (cli_command, dry_run, work_dir, task_timeout)
    - 2 new Prometheus metrics (cli_executions_total, cli_duration_seconds)
    - 10 new tests + 1 updated test
  key_links:
    - worker_client._execute_task -> subprocess.run
    - worker_client._execute_task -> _report_failure (all error paths)
    - worker_client._execute_task -> _report_complete (success + dry-run)
    - WorkerConfig.from_env -> MESH_CLI_COMMAND, MESH_DRY_RUN, MESH_WORK_DIR, MESH_TASK_TIMEOUT_S
---

<objective>Replace _execute_task stub with real subprocess CLI invocation, dry-run mode, guaranteed failure semantics, and full env-based configuration</objective>

<execution_context>
GSD execute-plan workflow. Summary template: 13-01-SUMMARY.md
</execution_context>

<context>
- src/router/worker_client.py -- WorkerConfig + MeshWorker with stub _execute_task
- src/router/metrics.py -- Prometheus metrics
- tests/router/test_worker_client.py -- Existing worker tests
</context>

<tasks>
  <task type="auto">
    <name>Add CLI execution metrics</name>
    <files>src/router/metrics.py</files>
    <action>
    Add to MeshMetrics:
    1. `cli_executions` Counter with label `result` (success/failure/timeout/not_found/dry_run)
    2. `cli_duration_seconds` Histogram with subprocess-appropriate buckets
    </action>
    <verify>python -c "from src.router.metrics import MeshMetrics; m = MeshMetrics(); assert 'cli_executions' in m.generate().decode()"</verify>
    <done>CLI execution metrics registered</done>
  </task>

  <task type="auto">
    <name>Extend WorkerConfig with CLI fields and rewrite _execute_task</name>
    <files>src/router/worker_client.py</files>
    <action>
    1. Add to WorkerConfig: cli_command, dry_run, work_dir, task_timeout
    2. Update from_env() to read MESH_CLI_COMMAND, MESH_DRY_RUN, MESH_WORK_DIR, MESH_TASK_TIMEOUT_S
    3. Extract _ack_task(task_id) -> bool
    4. Extract _report_complete(task_id, result) and _report_failure(task_id, error)
    5. Rewrite _execute_task:
       a. Validate payload.prompt (fail if missing)
       b. Build command with {account_profile} interpolation
       c. Dry-run path: log + report complete
       d. subprocess.run with capture_output, timeout, cwd
       e. Handle: exit 0 -> complete, exit != 0 -> failure, TimeoutExpired, FileNotFoundError
       f. Global except -> _report_failure (guarantee no stuck tasks)
    </action>
    <verify>grep "def _execute_task" src/router/worker_client.py</verify>
    <done>Full CLI invocation with guaranteed failure semantics</done>
  </task>

  <task type="auto">
    <name>Add CLI execution tests</name>
    <files>tests/router/test_worker_client.py</files>
    <action>
    TestCLIExecution class (10 tests):
    1. test_dry_run -- verify complete with dry_run=True in result
    2. test_missing_prompt_fails -- empty/missing payload.prompt -> failure
    3. test_missing_payload_fails -- no payload key -> failure
    4. test_subprocess_success -- mock exit 0 -> complete with output
    5. test_subprocess_failure -- mock exit 1 -> failure with stderr
    6. test_subprocess_timeout -- mock TimeoutExpired -> failure
    7. test_command_not_found -- mock FileNotFoundError -> failure
    8. test_output_truncation -- stdout > 4KB -> truncated to 4096
    9. test_cli_command_interpolation -- ccs {account_profile} -> ccs work
    10. test_config_from_env -- all env vars read correctly

    Update existing test_poll_executes_received_task:
    - Add payload.prompt to task fixture
    - Add dry_run=True to avoid real subprocess
    </action>
    <verify>python -m pytest tests/router/test_worker_client.py::TestCLIExecution -v</verify>
    <done>10 new tests + 1 updated test pass</done>
  </task>
</tasks>

<verification>
1. `python -m pytest tests/router/test_worker_client.py::TestCLIExecution -v` -- 10 tests pass
2. `python -m pytest tests/router/test_worker_client.py -v` -- all worker tests pass (including updated existing)
3. `grep "subprocess.run" src/router/worker_client.py` -- real invocation exists
4. `grep "dry_run" src/router/worker_client.py` -- dry-run path exists
5. `grep "_report_failure" src/router/worker_client.py` -- extracted helper exists
6. `python -m pytest tests/ -v` -- full suite (435 tests) passes
</verification>

<success_criteria>
1. Missing payload.prompt -> failure reported
2. Dry-run mode -> complete without subprocess
3. Successful subprocess -> complete with output
4. Failed subprocess -> failure with stderr
5. Timeout -> failure with timeout message
6. Command not found -> failure with command name
7. Output truncation works (4KB stdout, 2KB stderr)
8. {account_profile} interpolation works
9. All env vars read correctly by from_env()
10. All 435 tests pass
</success_criteria>

<output>Create 13-01-SUMMARY.md with execution results</output>
