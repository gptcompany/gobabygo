---
phase: 03-communication-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/router/models.py, src/router/db.py, src/router/comms.py, tests/router/test_comms.py]
autonomous: true
---

<objective>
Implement hierarchical communication policy enforcement for the mesh router.

Purpose: Ensure strict BOSS→PRESIDENT→WORKERS→PRESIDENT communication hierarchy. The router physically blocks any operation that violates the hierarchy — not a warning, a hard block. This is a non-negotiable requirement (COMMS-01).
Output: `comms.py` module with role-based authorization, model updates with role fields, and comprehensive tests.

**Integration note:** This phase builds internal Python modules. HTTP API endpoints exposing these functions to PRESIDENT/BOSS are Phase 4 (Event Bridge). For now, modules are validated via unit tests and importable by future API layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-communication-verification/CONTEXT.md

# Source files to understand before implementing:
@src/router/models.py
@src/router/fsm.py
@src/router/db.py
@src/router/worker_manager.py
@src/router/scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role fields to models + create comms.py module</name>
  <files>src/router/models.py, src/router/db.py, src/router/comms.py</files>
  <action>
**Model updates (models.py):**
- Add `CommunicationRole` enum with values: `boss`, `president`, `worker` (use str enum like existing enums)
- Add `role: str = CommunicationRole.WORKER.value` field to `Worker` model (default: worker)
- Add `created_by: Optional[str] = None` field to `Task` model (worker_id or role identifier of the entity that created the task)

**Schema updates (db.py):**
- Add `role TEXT NOT NULL DEFAULT 'worker'` column to `workers` table in schema
- Add `created_by TEXT` column to `tasks` table in schema

**Create comms.py module:**
Define the communication hierarchy enforcement:

```python
# Allowed communication edges (directed graph)
HIERARCHY_EDGES = {
    CommunicationRole.BOSS: {CommunicationRole.PRESIDENT},
    CommunicationRole.PRESIDENT: {CommunicationRole.WORKER, CommunicationRole.BOSS},
    CommunicationRole.WORKER: {CommunicationRole.PRESIDENT},
}
```

Implement `CommunicationPolicy` class with these methods:
1. `can_create_task(creator_role: str) -> bool` — Only boss/president can create tasks. Workers cannot create tasks for other workers.
2. `can_dispatch_task(dispatcher_role: str) -> bool` — Only president role dispatches tasks to workers (scheduler acts on behalf of president).
3. `can_ack_task(worker_id: str, task: Task) -> bool` — Only the assigned worker can ack their own task (check task.assigned_worker == worker_id).
4. `can_complete_task(worker_id: str, task: Task) -> bool` — Only the assigned worker can report completion (check task.assigned_worker == worker_id).
5. `can_view_all_tasks(role: str) -> bool` — Boss/president can view all tasks. Workers have scoped visibility.
6. `validate_communication(sender_role: str, receiver_role: str) -> bool` — Check if the sender→receiver edge exists in HIERARCHY_EDGES.

Each method returns bool. On False, the caller should raise/handle the violation. Keep the module simple — pure validation functions, no side effects.

**Important:**
- Follow existing code patterns: use `str` for role values (not enum objects) in model fields, matching the pattern used by `TaskStatus` and `CLIType`
- Do NOT modify any existing function signatures — this is additive only
- The `CommunicationPolicy` class should be stateless (no DB dependency) — it's a pure policy engine
  </action>
  <verify>
    - `python -c "from src.router.models import CommunicationRole; print(CommunicationRole.BOSS.value)"` prints "boss"
    - `python -c "from src.router.comms import CommunicationPolicy; p = CommunicationPolicy(); print(p.can_create_task('worker'))"` prints "False"
    - `python -c "from src.router.comms import CommunicationPolicy; p = CommunicationPolicy(); print(p.can_create_task('president'))"` prints "True"
  </verify>
  <done>
    - CommunicationRole enum exists with boss/president/worker values
    - Worker model has `role` field defaulting to "worker"
    - Task model has `created_by` field
    - DB schema includes new columns
    - comms.py module with CommunicationPolicy class implementing all 6 validation methods
    - All existing tests still pass (no regressions)
  </done>
</task>

<task type="auto">
  <name>Task 2: Communication policy tests</name>
  <files>tests/router/test_comms.py</files>
  <action>
Create comprehensive tests for the communication policy module.

**Test structure (follow existing test patterns from test_fsm.py, test_worker_manager.py):**

Test categories:
1. **Role validation tests:**
   - `test_can_create_task_boss` — boss can create → True
   - `test_can_create_task_president` — president can create → True
   - `test_can_create_task_worker` — worker cannot create → False
   - `test_can_dispatch_task_president` — president can dispatch → True
   - `test_can_dispatch_task_worker` — worker cannot dispatch → False
   - `test_can_dispatch_task_boss` — boss cannot dispatch (only president dispatches) → False

2. **Task ownership tests:**
   - `test_can_ack_task_assigned_worker` — assigned worker can ack → True
   - `test_can_ack_task_wrong_worker` — different worker cannot ack → False
   - `test_can_ack_task_unassigned` — no assigned worker → False
   - `test_can_complete_task_assigned_worker` — assigned worker can complete → True
   - `test_can_complete_task_wrong_worker` — different worker cannot → False

3. **Hierarchy edge tests:**
   - `test_validate_communication_boss_to_president` → True
   - `test_validate_communication_president_to_worker` → True
   - `test_validate_communication_worker_to_president` → True
   - `test_validate_communication_worker_to_worker` → False (no lateral)
   - `test_validate_communication_worker_to_boss` → False (skip hierarchy)
   - `test_validate_communication_boss_to_worker` → False (must go through president)

4. **Visibility tests:**
   - `test_can_view_all_tasks_boss` → True
   - `test_can_view_all_tasks_president` → True
   - `test_can_view_all_tasks_worker` → False

Use pytest fixtures for Task and Worker instances (follow pattern from existing tests).
Run full test suite after to verify no regressions.
  </action>
  <verify>
    - `cd /media/sam/1TB/gobabygo && python -m pytest tests/router/test_comms.py -v` — all tests pass
    - `cd /media/sam/1TB/gobabygo && python -m pytest tests/ -v` — full suite passes (no regressions), count >= 92 + new tests
  </verify>
  <done>
    - test_comms.py with ~18 tests covering all policy methods
    - All new tests pass
    - Full test suite (92 existing + new) passes with 0 failures
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m pytest tests/ -v` — all tests pass (existing 92 + new ~18)
- [ ] `python -c "from src.router.comms import CommunicationPolicy"` — module imports cleanly
- [ ] `python -c "from src.router.models import CommunicationRole"` — enum available
- [ ] No modifications to existing function signatures
- [ ] Existing test count unchanged (92 tests still pass)
</verification>

<success_criteria>

- CommunicationPolicy enforces strict BOSS→PRESIDENT→WORKERS→PRESIDENT hierarchy
- Workers cannot create tasks, dispatch tasks, or communicate laterally
- Only assigned workers can ack/complete their own tasks
- All 6 policy methods implemented and tested
- Zero regressions on existing 92 tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-communication-verification/03-01-SUMMARY.md`
</output>
