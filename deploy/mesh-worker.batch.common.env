# Shared defaults for batch workers on WS.
#
# Install to /etc/mesh-worker/mesh-worker.batch.common.env and keep it outside git
# on live hosts once customized.

MESH_LONGPOLL_TIMEOUT_S=25
MESH_DRY_RUN=1
MESH_WORK_DIR=/tmp/mesh-tasks
MESH_TASK_TIMEOUT_S=1800
